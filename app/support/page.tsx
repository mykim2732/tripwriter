
"use client";
import { Headphones, Send } from "lucide-react";
import { useState } from "react";
import { PageShell } from "@/components/PageShell";
const types = ["Payment", "Login", "Generation", "Photo upload", "Publishing", "Other"];
const faqs = ["Credits are used for AI generation, AI design, and photo analysis.", "Automatic publishing is coming later. You can copy and publish manually for now.", "Social login uses Supabase OAuth settings."];
export default function SupportPage() { const [type, setType] = useState(types[0]); const [message, setMessage] = useState(""); const [done, setDone] = useState(false); function submit() { window.localStorage.setItem("posty-support-draft", JSON.stringify({ type, message, createdAt: new Date().toISOString() })); setDone(true); setMessage(""); } return <PageShell><section className="px-5 pb-8 pt-7"><div className="mb-6"><p className="text-sm font-bold text-blue-600">Posty AI Support</p><h1 className="mt-2 text-3xl font-black tracking-normal text-slate-950">Support</h1><p className="mt-2 text-sm leading-6 text-slate-500">Read FAQs or leave a support draft.</p></div><div className="space-y-4"><article className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-100"><div className="flex items-center gap-2"><Headphones className="text-blue-600" size={21} /><h2 className="text-base font-black text-slate-950">FAQ</h2></div><div className="mt-3 space-y-2">{faqs.map((faq) => <p key={faq} className="rounded-2xl bg-slate-50 px-4 py-3 text-sm font-bold leading-6 text-slate-600">{faq}</p>)}</div></article><article className="rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-100"><h2 className="text-base font-black text-slate-950">Contact</h2><select value={type} onChange={(event) => setType(event.target.value)} className="mt-3 h-12 w-full rounded-2xl bg-slate-50 px-3 text-sm font-bold text-slate-700 outline-none">{types.map((item) => <option key={item}>{item}</option>)}</select><textarea value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Write your question. Email delivery will be connected later." className="mt-3 min-h-36 w-full rounded-2xl bg-slate-50 p-3 text-sm leading-6 text-slate-800 outline-none" /><button type="button" onClick={submit} className="mt-3 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-2xl bg-blue-600 px-4 text-sm font-black text-white"><Send size={17} /> Save inquiry</button>{done && <p className="mt-3 rounded-2xl bg-blue-50 px-4 py-3 text-sm font-bold text-blue-700">Inquiry draft saved.</p>}</article></div></section></PageShell>; }
